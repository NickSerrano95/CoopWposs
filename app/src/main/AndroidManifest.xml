<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coopwposs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoopWposs">
        <activity android:name=".Lista_Usuarios"></activity>
        <activity android:name=".Registro" />
        <activity android:name=".AdministrarCuentas" />
        <activity android:name=".Depositos" />
        <activity android:name=".Transferencias" />
        <activity android:name=".Retiros" />
        <activity android:name=".Acceso_normal" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>